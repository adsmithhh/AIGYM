agent:
  id: "agent.minimal.v1"
  name: "MinimalAgent"
  capabilities: []
  needs: {}
  handshake:
    protocol: "aigym/1.0"
    intents: ["cooperate"]

tasks:
  - id: "task.echo"
    description: "Minimal echo"
    adapter: "echo_v1"

adapters:
  echo_v1:
    type: "echo"
    params: { prefix: "[MIN]" }
